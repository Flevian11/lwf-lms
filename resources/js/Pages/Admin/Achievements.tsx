import { Head, router } from '@inertiajs/react'
import { useMemo, useState } from 'react'
import AdminLayout from '../../Components/AdminLayout'
import { AdminCard, Doughnut3D, Field, MiniIcon, Modal, Pill, PremiumStat, inputClass, textareaClass } from '../../Components/AdminPremiumUI'

type Achievement = { id:number; name:string; slug:string; description:string|null; icon:string|null; points:number; criteria_type:string; is_active:boolean; sort_order:number; awarded_count:number; created_at:string; updated_at:string }
type Student = { id:number; name:string; email:string }
type Paginator<T> = { data:T[]; current_page:number; last_page:number; from:number|null; to:number|null; total:number }
type Props = { admin:any; achievements:Paginator<Achievement>; students:Student[]; stats:{total:number;active:number;awarded:number;points:number}; filters:{search:string;status:string} }

const iconChoices = ['trophy','award','shield','star','spark','target']

export default function Achievements({ admin, achievements, students, stats, filters }: Props) {
    const [search, setSearch] = useState(filters.search ?? '')
    const [status, setStatus] = useState(filters.status ?? 'all')
    const [showCreate, setShowCreate] = useState(false)
    const [editing, setEditing] = useState<Achievement|null>(null)
    const [awarding, setAwarding] = useState<Achievement|null>(null)

    const applyFilters = (nextSearch = search, nextStatus = status) => router.get('/admin/achievements', { search: nextSearch || undefined, status: nextStatus !== 'all' ? nextStatus : undefined }, { preserveState:true, preserveScroll:true, replace:true })
    const activePct = stats.total ? Math.round((stats.active / stats.total) * 100) : 0

    return <>
        <Head title="Achievements · Administration" />
        <AdminLayout admin={admin} title="Achievements">
            <div className="space-y-5">
                <header className="flex flex-col gap-4 px-1 pt-1 sm:flex-row sm:items-end sm:justify-between">
                    <div>
                        <p className="text-[9px] font-black uppercase tracking-[.18em] text-[#1554c0] dark:text-[#79a8ff]">Recognition system</p>
                        <h1 className="mt-1 text-[26px] font-black tracking-[-.04em] text-slate-950 dark:text-white sm:text-[26px]">Achievements</h1>
                        <p className="mt-1 max-w-3xl text-[11px] leading-5 text-slate-500 dark:text-slate-400">Define meaningful learner milestones and award standout students from a controlled, auditable administrative workflow.</p>
                    </div>
                    <button onClick={()=>setShowCreate(true)} className="inline-flex h-10 cursor-pointer items-center justify-center gap-2 rounded-xl bg-[#1554c0] px-4 text-[10px] font-black text-white shadow-[0_12px_28px_-16px_rgba(21,84,192,.9)] transition hover:-translate-y-0.5 hover:bg-[#1048a8]"><MiniIcon name="plus" className="h-3.5 w-3.5"/> New achievement</button>
                </header>

                <div className="grid grid-cols-2 gap-3 xl:grid-cols-4"><PremiumStat label="Achievement definitions" value={stats.total} detail={`${stats.active} currently active`} icon="award"/><PremiumStat label="Active recognition" value={`${activePct}%`} detail="Definitions available to admins" icon="shield" accent="violet"/><PremiumStat label="Awards issued" value={stats.awarded} detail="Student achievement records" icon="users" accent="emerald"/><PremiumStat label="Points represented" value={stats.points} detail="Across active definitions" icon="chart" accent="amber"/></div>

                <div className="grid grid-cols-1 gap-5 xl:grid-cols-[minmax(0,1.55fr)_360px]">
                    <AdminCard className="overflow-hidden p-0">
                        <div className="border-b border-slate-100 p-5 dark:border-slate-800 sm:p-5"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><h2 className="text-base font-black tracking-tight text-slate-950 dark:text-white">Recognition library</h2><p className="mt-1 text-[11px] text-slate-500 dark:text-slate-400">The awards administrators can use across the learner population.</p></div><div className="flex flex-col gap-2 sm:flex-row"><div className="relative min-w-0 sm:w-64"><MiniIcon name="search" className="absolute left-3 top-3 h-4 w-4 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>e.key==='Enter'&&applyFilters()} placeholder="Search recognition..." className={inputClass('pl-9')}/></div><select value={status} onChange={e=>{setStatus(e.target.value);applyFilters(search,e.target.value)}} className={inputClass('sm:w-36')}><option value="all">All statuses</option><option value="active">Active</option><option value="inactive">Inactive</option></select></div></div></div>
                        <div className="divide-y divide-slate-100 dark:divide-slate-800">
                            {achievements.data.map((item, index)=><div key={item.id} className="group p-5 transition hover:bg-slate-50/70 dark:hover:bg-slate-900/45 sm:p-5"><div className="flex flex-col gap-5 lg:flex-row lg:items-center"><div className="flex min-w-0 flex-1 items-center gap-4"><div className="relative flex h-14 w-14 shrink-0 items-center justify-center rounded-[18px] bg-gradient-to-br from-amber-50 via-white to-violet-50 text-amber-600 shadow-sm ring-1 ring-amber-200/60 dark:from-amber-500/10 dark:via-slate-900 dark:to-violet-500/10 dark:text-amber-300 dark:ring-amber-500/10"><MiniIcon name={item.icon || 'trophy'} className="h-6 w-6"/><span className="absolute -right-1 -top-1 flex h-5 w-5 items-center justify-center rounded-full bg-[#10213f] text-[8px] font-black text-white">{index+1}</span></div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="truncate text-sm font-black text-slate-900 dark:text-white">{item.name}</h3><Pill tone={item.is_active?'green':'slate'}>{item.is_active?'Active':'Inactive'}</Pill></div><p className="mt-1 line-clamp-2 text-[11px] leading-5 text-slate-500 dark:text-slate-400">{item.description || 'Recognition for exceptional learner progress.'}</p><div className="mt-2 flex flex-wrap gap-2"><span className="rounded-lg bg-amber-50 px-2 py-1 text-[9px] font-bold text-amber-700 dark:bg-amber-500/10 dark:text-amber-300">+{item.points} points</span><span className="rounded-lg bg-slate-100 px-2 py-1 text-[9px] font-bold text-slate-500 dark:bg-slate-800 dark:text-slate-300">{item.criteria_type === 'manual' ? 'Manual award' : 'System milestone'}</span><span className="rounded-lg bg-slate-100 px-2 py-1 text-[9px] font-bold text-slate-500 dark:bg-slate-800 dark:text-slate-300">{item.awarded_count} awarded</span></div></div></div><div className="flex shrink-0 items-center gap-2 lg:justify-end"><button onClick={()=>setEditing(item)} className="h-9 rounded-xl border border-slate-200 px-3 text-[10px] font-bold text-slate-600 transition hover:border-[#1554c0] hover:text-[#1554c0] dark:border-slate-700 dark:text-slate-300">Edit</button><button onClick={()=>setAwarding(item)} disabled={!item.is_active} className="h-9 rounded-xl bg-[#1554c0] px-3.5 text-[10px] font-black text-white shadow-sm transition hover:bg-[#1048a8] disabled:cursor-not-allowed disabled:opacity-40">Award to student</button></div></div></div>)}
                            {!achievements.data.length && <div className="p-12 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-[#edf4ff] text-[#1554c0] dark:bg-[#172945]"><MiniIcon name="trophy"/></div><h3 className="mt-4 text-sm font-black text-slate-900 dark:text-white">No recognition definitions found</h3><p className="mx-auto mt-1 max-w-md text-xs leading-5 text-slate-500">Create the first award definition and it will appear here.</p></div>}
                        </div>
                        {achievements.last_page > 1 && <div className="flex items-center justify-between border-t border-slate-100 px-5 py-4 dark:border-slate-800"><span className="text-[10px] font-semibold text-slate-400">Showing {achievements.from ?? 0}–{achievements.to ?? 0} of {achievements.total}</span><div className="flex gap-2">{[...Array(achievements.last_page)].map((_,i)=><button key={i} onClick={()=>router.get('/admin/achievements',{search:search||undefined,status:status!=='all'?status:undefined,page:i+1},{preserveScroll:true,preserveState:true})} className={`h-8 min-w-8 rounded-lg px-2 text-[10px] font-bold ${achievements.current_page===i+1?'bg-[#1554c0] text-white':'border border-slate-200 text-slate-500 dark:border-slate-700 dark:text-slate-300'}`}>{i+1}</button>)}</div></div>}
                    </AdminCard>
                    <div className="space-y-5"><AdminCard className="p-5 sm:p-5"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.13em] text-slate-400">Recognition health</p><h3 className="mt-1 text-base font-black text-slate-900 dark:text-white">Active library</h3></div><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-emerald-50 text-emerald-600 dark:bg-emerald-500/10 dark:text-emerald-300"><MiniIcon name="chart"/></div></div><div className="mt-5"><Doughnut3D value={stats.active} total={stats.total} label="Active definitions" sublabel="Keep the recognition catalogue intentional and current." tone="green"/></div></AdminCard><AdminCard className="relative overflow-hidden p-5 sm:p-5"><div className="absolute -right-12 -top-12 h-32 w-32 rounded-full bg-violet-500/10 blur-3xl"/><div className="relative"><p className="text-[10px] font-bold uppercase tracking-[.13em] text-slate-400">Admin principle</p><h3 className="mt-2 text-lg font-black text-slate-900 dark:text-white">Celebrate the learner, not the form.</h3><p className="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400">Awarding is intentionally separated from definition management. An administrator selects a real student, chooses a real achievement and records an optional note.</p><div className="mt-5 flex items-center gap-3 rounded-2xl bg-slate-50 p-3 dark:bg-slate-900/70"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-white text-[#1554c0] shadow-sm dark:bg-slate-800"><MiniIcon name="award" className="h-4 w-4"/></div><div><p className="text-[10px] font-black text-slate-800 dark:text-white">Technical fields stay hidden</p><p className="text-[9px] text-slate-400">The admin workflow stays focused on recognition.</p></div></div></div></AdminCard></div>
                </div>
            </div>
        </AdminLayout>
        {showCreate && <AchievementForm onClose={()=>setShowCreate(false)} mode="create"/>}
        {editing && <AchievementForm item={editing} onClose={()=>setEditing(null)} mode="edit"/>}
        {awarding && <AwardModal item={awarding} students={students} onClose={()=>setAwarding(null)}/>} 
    </>
}

function AchievementForm({ mode, item, onClose }: { mode:'create'|'edit'; item?:Achievement; onClose:()=>void }) {
    const [name,setName]=useState(item?.name??''); const [description,setDescription]=useState(item?.description??''); const [points,setPoints]=useState(String(item?.points??10)); const [icon,setIcon]=useState(item?.icon??'trophy'); const [active,setActive]=useState(item?.is_active??true); const [processing,setProcessing]=useState(false)
    const submit=(e:any)=>{e.preventDefault();setProcessing(true);const data={name,description,points:Number(points),icon,is_active:active,sort_order:item?.sort_order??0}; const options={preserveScroll:true,onFinish:()=>setProcessing(false),onSuccess:onClose}; mode==='create'?router.post('/admin/achievements',data,options):router.put(`/admin/achievements/${item!.id}`,data,options)}
    return <Modal title={mode==='create'?'Create achievement':'Edit achievement'} eyebrow="Recognition definition" onClose={onClose}><form onSubmit={submit} className="space-y-5"><div className="grid gap-4 sm:grid-cols-[1fr_160px]"><Field label="Achievement name"><input required value={name} onChange={e=>setName(e.target.value)} className={inputClass()} placeholder="Best Student"/></Field><Field label="Points"><input required min="0" type="number" value={points} onChange={e=>setPoints(e.target.value)} className={inputClass()}/></Field></div><Field label="Description"><textarea value={description} onChange={e=>setDescription(e.target.value)} className={textareaClass()} placeholder="Recognises exceptional academic effort, consistency or contribution."/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Badge icon"><div className="grid grid-cols-3 gap-2">{iconChoices.map(choice=><button type="button" key={choice} onClick={()=>setIcon(choice)} className={`flex h-12 items-center justify-center rounded-xl border transition ${icon===choice?'border-[#1554c0] bg-[#edf4ff] text-[#1554c0] dark:bg-[#172945] dark:text-[#8bb5ff]':'border-slate-200 text-slate-400 dark:border-slate-700'}`}><MiniIcon name={choice}/></button>)}</div></Field><Field label="Availability"><button type="button" onClick={()=>setActive(v=>!v)} className={`flex h-11 w-full items-center justify-between rounded-xl border px-3.5 text-xs font-bold ${active?'border-emerald-200 bg-emerald-50 text-emerald-700 dark:border-emerald-500/20 dark:bg-emerald-500/10 dark:text-emerald-300':'border-slate-200 bg-slate-50 text-slate-500 dark:border-slate-700 dark:bg-slate-900'}`}><span>{active?'Available for awarding':'Hidden from awarding'}</span><span className={`h-5 w-9 rounded-full p-0.5 ${active?'bg-emerald-500':'bg-slate-300 dark:bg-slate-700'}`}><span className={`block h-4 w-4 rounded-full bg-white transition ${active?'translate-x-4':''}`}/></span></button></Field></div><div className="rounded-2xl border border-[#dfe8f7] bg-[#f7faff] p-4 dark:border-slate-800 dark:bg-slate-900/60"><div className="flex gap-3"><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-white text-[#1554c0] shadow-sm dark:bg-slate-800"><MiniIcon name="shield" className="h-4 w-4"/></div><div><p className="text-[10px] font-black text-slate-800 dark:text-white">Manual recognition definition</p><p className="mt-1 text-[10px] leading-4 text-slate-500 dark:text-slate-400">This admin-created award is stored as a manual recognition. Technical criteria remain internal to the system.</p></div></div></div><div className="flex justify-end gap-2 pt-2"><button type="button" onClick={onClose} className="h-10 cursor-pointer rounded-xl border border-slate-200 px-4 text-xs font-bold text-slate-600 dark:border-slate-700 dark:text-slate-300">Cancel</button><button disabled={processing} className="h-10 cursor-pointer rounded-xl bg-[#1554c0] px-5 text-xs font-black text-white shadow-sm disabled:opacity-50">{processing?'Saving...':mode==='create'?'Create achievement':'Save changes'}</button></div></form></Modal>
}

function AwardModal({item,students,onClose}:{item:Achievement;students:Student[];onClose:()=>void}) { const [student,setStudent]=useState('');const [note,setNote]=useState('');const [processing,setProcessing]=useState(false);const submit=(e:any)=>{e.preventDefault();if(!student)return;setProcessing(true);router.post(`/admin/achievements/${item.id}/award`,{user_id:Number(student),note},{preserveScroll:true,onFinish:()=>setProcessing(false),onSuccess:onClose})}; return <Modal title="Award achievement" eyebrow="Administrator recognition" onClose={onClose}><div className="mb-5 rounded-[20px] bg-gradient-to-br from-[#10213f] to-[#263d75] p-5 text-white shadow-lg"><div className="flex items-center gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-white/10 ring-1 ring-white/10"><MiniIcon name={item.icon||'trophy'} className="h-7 w-7"/></div><div><p className="text-[9px] font-bold uppercase tracking-[.15em] text-blue-200">Recognition</p><h3 className="mt-1 text-lg font-black">{item.name}</h3><p className="mt-1 text-[10px] text-blue-100/70">+{item.points} points · awarded once per student</p></div></div></div><form onSubmit={submit} className="space-y-5"><Field label="Student"><select required value={student} onChange={e=>setStudent(e.target.value)} className={inputClass()}><option value="">Select a student...</option>{students.map(s=><option key={s.id} value={s.id}>{s.name} · {s.email}</option>)}</select></Field><Field label="Recognition note" hint="Optional. This note is retained as award metadata for the administrator record."><textarea value={note} onChange={e=>setNote(e.target.value)} className={textareaClass()} placeholder="Why is this student receiving the award?"/></Field><div className="flex justify-end gap-2"><button type="button" onClick={onClose} className="h-10 cursor-pointer rounded-xl border border-slate-200 px-4 text-xs font-bold text-slate-600 dark:border-slate-700 dark:text-slate-300">Cancel</button><button disabled={!student||processing} className="h-10 cursor-pointer rounded-xl bg-[#1554c0] px-5 text-xs font-black text-white disabled:opacity-50">{processing?'Awarding...':'Award achievement'}</button></div></form></Modal> }
